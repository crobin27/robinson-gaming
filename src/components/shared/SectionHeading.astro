---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-12">
  <h2 class="text-3xl font-bold tracking-tight md:text-4xl">{title}</h2>
  {subtitle && <p class="mt-3 text-lg text-muted-foreground">{subtitle}</p>}
</div>
